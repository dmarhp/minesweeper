export enum GameStatus {
    Play,
    Pause,
    Win,
    Lose,
}

export enum Difficulty {
    Beginner,
    Intermediate,
    Expert,
    Advanced,
}

export enum GameStage {
    Settings,
    Game,
}

export enum Icon {
    ArrowDown,
    Check,
    FlagWin,
    Home,
    Settings,
    Pause,
    Play,
    X,

    Mine,
    Flag,

    Neighbour1,
    Neighbour2,
    Neighbour3,
    Neighbour4,
    Neighbour5,
    Neighbour6,
    Neighbour7,
    Neighbour8,

    FaceSmile,
    FaceNeutral,
    FaceWin,
    FaceLose,
}

export enum ChangelogItemType {
    New,
    Improved,
    Fixed,
}
